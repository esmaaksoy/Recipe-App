describe('login', () => {
    // beforeEach(() => {
    //   cy.visit('http://localhost:3000/login')
    //   cy.viewport(1200,1200);
    // })
    it('login test', () => {
   cy.login()
    //   cy.get('[data-test="menutest"]').should('be.visible').click({ force: true })
    })
  
  })